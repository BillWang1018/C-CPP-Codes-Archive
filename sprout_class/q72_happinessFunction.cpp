#include <iostream>
using namespace std;

double f(double a, double b, double t, double c) {
    return a * (t-b) * (t-b) + c;
}

int main() {
    int T;
    cin >> T;
    while(T--) {
        int n, a, b, t, c;
        cin >> n;
        
    }
    return 0;
}